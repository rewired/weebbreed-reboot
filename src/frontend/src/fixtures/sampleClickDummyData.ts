import { createClickDummyFixture } from './clickDummyFactories';

const fixture = createClickDummyFixture();

export const CLICKDUMMY_SAMPLE = fixture.data;
export const CLICKDUMMY_SAMPLE_CONTEXT = fixture.context;
